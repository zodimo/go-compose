# Layout Node

Is the core UI Tree Node. Carrying information about itself and it's children.
It uses Layout Elements to construct the UI.

# Modifiers
Modifiers can Modifier the Node's behavior by adding and removing Layout Elements and or children.
Even wrap the owner node itself
